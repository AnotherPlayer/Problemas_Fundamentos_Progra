# Problemas de Fundamentos de Programación en C

Este repositorio contiene una serie de ejercicios y programas básicos desarrollados en lenguaje C, que abarcan desde conceptos iniciales hasta el uso de arreglos y apuntadores.

## 🛠️ Compilar y ejecutar desde terminal

Para poner en marcha estos programas, sigue estos pasos:

1.  **Abrir terminal** en la ubicación del archivo `.c`.
2.  **Compilar el código** usando el siguiente comando:
    ```bash
    gcc "nombre_archivo".c -o "nombre_ejecutable".exe
    ```
3.  **Ejecutar el .exe** con el siguiente comando:
    ```bash
    ./"nombre_ejecutable".exe
    ```

### Lista de comandos útiles
* `clear`: Limpiar la consola.
* `ls`: Muestra el contenido dentro de la carpeta actual.
* `cd "ruta"`: Moverte entre carpetas.

## ⚙️ Configuración adicional
* **VS Code**: Se incluye un archivo `tasks.json` para automatizar la compilación con GCC desde el editor.
* **Git**: El archivo `.gitignore` está configurado para no subir los archivos ejecutables `.exe` al repositorio.